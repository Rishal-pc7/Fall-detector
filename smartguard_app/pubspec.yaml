name: smartguard_app
description: "SmartGuard AI Fall Detection Mobile Companion App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^4.14.0
  firebase_database: ^12.5.0
  flutter_local_notifications: ^22.3.0
  intl: ^0.20.2
  cached_network_image: ^4.0.0
  google_fonts: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
